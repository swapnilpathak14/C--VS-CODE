#include <bits/stdc++.h>
using namespace std;
int main(){
    long long int t;
    cin>>t;
    while(t--){
        int n, k;
        cin >> n >> k;
        string s;
        getline(cin,s);
        
        //define goodness
        for(int x=1;x<=s.length();x++){
            if(s[x]!=s[n-x+1]){

            }
        }
    }
}